# Use a Library with standalone documents

Paperbranch will restore one user-chosen Library when it launches and mirror that folder's hierarchy in its sidebar. The Library lives in one Library window. A Markdown document opened from Finder outside the Library remains a standalone document, is not added to the Library, and opens in its own Standalone window. This keeps the persistent collection separate from one-off files.
